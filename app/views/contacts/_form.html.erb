<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vorname %>
    <%= form.text_field :vorname, id: :contact_vorname %>
  </div>

  <div class="field">
    <%= form.label :nachname %>
    <%= form.text_field :nachname, id: :contact_nachname %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :contact_email %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :contact_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
